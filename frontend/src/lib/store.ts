// lib/store.ts - FOLLOWS HACKATHON RULES
// Placeholder file for future Zustand stores
// Currently all state management is handled by individual hooks

// Helper to get current store state (useful for debugging)
export const getStoreState = () => ({
  // Add store getters here when stores are implemented
})
